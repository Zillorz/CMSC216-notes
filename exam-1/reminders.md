# Exam 1 - extra

This is a list of things I think could be tricky or complicated on exam 1

**IMPORTANT**
- `free()` everything you `malloc()`
- `fclose()` everything you `fopen()`
- *DO THIS ON EARLY RETURNS TOO!*

### Copy Semantics
- Everything is a DIRECT copy
- Use a for loop / `memcpy()` / `strcpy()` on pointers and arrays

###
